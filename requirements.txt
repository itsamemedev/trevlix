# ╔══════════════════════════════════════════════════════════════╗
# ║  TREVLIX v1.5.0 – Python Dependencies                       ║
# ║  Installieren: pip install -r requirements.txt               ║
# ╚══════════════════════════════════════════════════════════════╝

# ── Core Web Framework ─────────────────────────────────────────
flask>=3.0.0,<4.0.0
flask-socketio>=5.3.6,<6.0.0
flask-cors>=4.0.0,<5.0.0
flask-limiter>=3.5.0,<4.0.0
simple-websocket>=1.0.0,<2.0.0   # WebSocket-Transport für async_mode="threading"
# eventlet entfernt: Server nutzt async_mode="threading" + Werkzeug (allow_unsafe_werkzeug=True)

# ── Exchange Connectivity ──────────────────────────────────────
ccxt>=4.2.0,<5.0.0

# ── Database ───────────────────────────────────────────────────
PyMySQL>=1.5.0,<2.0.0
cryptography>=41.0.0,<44.0.0   # Fernet API-Key-Verschlüsselung

# ── Data Processing ────────────────────────────────────────────
pandas>=2.1.0,<3.0.0
numpy>=1.26.0,<2.0.0
requests>=2.31.0,<3.0.0
httpx>=0.26.0,<1.0.0           # Moderner async HTTP-Client (Verbesserung #14)

# ── Machine Learning ───────────────────────────────────────────
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<3.0.0
lightgbm>=4.0.0,<5.0.0        # Schnelles Gradient Boosting
catboost>=1.2.0,<2.0.0        # Gradient Boosting mit Kategorie-Support
joblib>=1.3.0,<2.0.0          # Model-Persistenz & paralleles Rechnen
optuna>=3.5.0,<5.0.0          # Bayesian Hyperparameter-Optimierung (Verbesserung #15)
PyWavelets>=1.5.0,<2.0.0      # Wavelet-Dekomposition für spektrale Features (server.py)
imbalanced-learn>=0.11.0,<0.13.0  # SMOTE Oversampling für Klassen-Imbalance (server.py)

# ── Auth & Security ────────────────────────────────────────────
PyJWT>=2.8.0,<3.0.0
bcrypt>=4.1.0,<5.0.0
pyotp>=2.9.0,<3.0.0           # 2FA TOTP
python-dotenv>=1.0.0,<2.0.0

# ── Deep Learning (optional – LSTM + Transformer) ─────────────
# Installieren mit: pip install tensorflow
# tensorflow>=2.15.0

# ── Dev / Testing ─────────────────────────────────────────────
pytest>=7.4.0,<9.0.0          # Unit-Tests
pytest-cov>=4.1.0,<6.0.0      # Test-Coverage
